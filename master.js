import '.lib/performance.js'

// run all these tests together
// look at tags on functions and run if they have the tag end2end
export runAllTests function() {
    //logic to only run the two above based on tags
    // loops through all tests
}

// function to run only one test
